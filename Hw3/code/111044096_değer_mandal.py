import math #calculates the shapes areas
z = float(input('enter the length of one side of square')) #ask one side of square
a=(z*z) #formula of the square
print(a) #calculate
r = float(input('enter the radius value of the circle')) #ask length of the radius value
q=(math.pi)*r*r #formula of the circle
print(q) #calculate
h = float(input('enter the length of one side of triangle')) #ask one side of triangle
k=(math.sqrt(3))*h*h/4 #formula of the triangle
print(k) #calculate
input('the end')
